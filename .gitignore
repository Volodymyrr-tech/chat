# Ignore node_modules
node_modules/

# Ignore the .git folder (Git version control)
.git

# Ignore Dockerfile and docker-compose files (if not needed inside the container)
Dockerfile
docker-compose.yml

# Ignore logs and temporary files
npm-debug.log
yarn-debug.log
yarn-error.log
*.log

# Ignore environment variable files (if you're not including them in the image)
.env

# Ignore package manager lock files (optional if you don't want them in the image)
package-lock.json
yarn.lock

# Ignore any OS-specific files (macOS specific)
.DS_Store
